---
trigger: always_on
---

Version: Focus on Angular 21 features.

State Management: Use Signals for all reactive states. Avoid BehaviorSubject.

Data Fetching: Use the new httpResource API for all backend communications.

Components: Always use Standalone Components with OnPush change detection strategy.

Template: Use the new control flow syntax (@if, @for).
